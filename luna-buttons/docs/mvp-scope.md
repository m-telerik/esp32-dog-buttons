# MVP Scope

- Support for up to 16 buttons using a multiplexer
- Audio playback of pre-recorded sound files when buttons are pressed
- Each button plays its corresponding audio file (e.g., "potty" button → potty.wav)
- Webhook integration to log button press events
- Real-time notifications via Telegram
